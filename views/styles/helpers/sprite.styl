/*
Stylus variables are information about icon's compiled state, stored under its original file name

.icon-home {
  width: $icon_home_width;
}

The large array-like variables contain all information about a single icon
$icon_home = x y offset_x offset_y width height total_width total_height image_path;

At the bottom of this section, we provide information about the spritesheet itself
$spritesheet = width height image $spritesheet_sprites;
*/
$arrow_270_name = 'arrow-270';
$arrow_270_x = 0px;
$arrow_270_y = 0px;
$arrow_270_offset_x = 0px;
$arrow_270_offset_y = 0px;
$arrow_270_width = 32px;
$arrow_270_height = 32px;
$arrow_270_total_width = 72px;
$arrow_270_total_height = 56px;
$arrow_270_image = '../images/sprite.png';
$arrow_270 = 0px 0px 0px 0px 32px 32px 72px 56px '../images/sprite.png' 'arrow-270';
$arrow_name = 'arrow';
$arrow_x = 40px;
$arrow_y = 0px;
$arrow_offset_x = -40px;
$arrow_offset_y = 0px;
$arrow_width = 32px;
$arrow_height = 32px;
$arrow_total_width = 72px;
$arrow_total_height = 56px;
$arrow_image = '../images/sprite.png';
$arrow = 40px 0px -40px 0px 32px 32px 72px 56px '../images/sprite.png' 'arrow';
$document_code_name = 'document-code';
$document_code_x = 0px;
$document_code_y = 40px;
$document_code_offset_x = 0px;
$document_code_offset_y = -40px;
$document_code_width = 16px;
$document_code_height = 16px;
$document_code_total_width = 72px;
$document_code_total_height = 56px;
$document_code_image = '../images/sprite.png';
$document_code = 0px 40px 0px -40px 16px 16px 72px 56px '../images/sprite.png' 'document-code';
$folder_name = 'folder';
$folder_x = 24px;
$folder_y = 40px;
$folder_offset_x = -24px;
$folder_offset_y = -40px;
$folder_width = 16px;
$folder_height = 16px;
$folder_total_width = 72px;
$folder_total_height = 56px;
$folder_image = '../images/sprite.png';
$folder = 24px 40px -24px -40px 16px 16px 72px 56px '../images/sprite.png' 'folder';
$spritesheet_width = 72px;
$spritesheet_height = 56px;
$spritesheet_image = '../images/sprite.png';
$spritesheet_sprites = $arrow_270 $arrow $document_code $folder;
$spritesheet = 72px 56px '../images/sprite.png' $spritesheet_sprites;

/*
The provided mixins are intended to be used with the array-like variables

.icon-home {
  spriteWidth($icon_home)
}

.icon-email {
  sprite($icon_email)
}
*/
spriteWidth($sprite) {
  width: $sprite[4];
}

spriteHeight($sprite) {
  height: $sprite[5];
}

spritePosition($sprite) {
  background-position: $sprite[2] $sprite[3];
}

spriteImage($sprite) {
  background-image: url($sprite[8]);
}

sprite($sprite) {
  spriteImage($sprite)
  spritePosition($sprite)
  spriteWidth($sprite)
  spriteHeight($sprite)
}

/*
The `sprites` mixin generates identical output to the CSS template
  but can be overridden inside of Stylus

This must be run when you have at least 2 sprites.
  If run with a single sprite, then there will be reference errors.

sprites($spritesheet_sprites);
*/
sprites($sprites) {
  for $sprite in $sprites {
    $sprite_name = $sprite[9];
    .{$sprite_name} {
      sprite($sprite);
    }
  }
}
